# Deprecated

The old multi-level generation harness has been removed.

Current behavior:
- one script: `create_presentation.py`
- no external image generation
- no image replacement flow
- no prompt registry
- slides are generated creatively from the topic itself

See `README.md` for the current workflow.
